// import React from 'react';
// import { shallow } from 'enzyme';

// import MovieDetailComponent from '../index';

describe('<MovieDetailComponent />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
